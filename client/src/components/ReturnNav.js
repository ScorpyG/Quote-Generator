import { Link } from "react-router-dom";

const ReturnNav = () => {
  return (
    <Link to="/" className="nav-btn bt2">
      <i className="fas fa-undo" />
    </Link>
  );
};

export default ReturnNav;
